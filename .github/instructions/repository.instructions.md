---
applyTo: '**'
---
# Command line tool instructions

Follow these instructions when using the command line tool `kicktippbb.py`:

* Always use `ehonda-test` as the community
* Call without any login arguments, as the tool will automatically use the token from `.env`
